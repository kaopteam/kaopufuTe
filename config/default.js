const config = {
  // 启动端口
  port: 3003,
  // 数据库配置
  database: {
    DATABASE: 'nodesql',
    USERNAME: 'root',
    PASSWORD: '123qwe',
    PORT: '3306',
    HOST: 'localhost'
  }
}

module.exports = config